# --- Supabase (public) ---
NEXT_PUBLIC_SUPABASE_URL=https://rxqbguwxgxxxsrrkjadc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_SUPABASE_ANON_KEY

# --- Supabase (server-only, never expose to the client) ---
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SUPABASE_SERVICE_ROLE_KEY

# --- Deposit verification (requires real values before go-live) ---
TRONGRID_API_KEY=REPLACE_WITH_TRONGRID_API_KEY

# --- Safe defaults ---
NODE_ENV=production
